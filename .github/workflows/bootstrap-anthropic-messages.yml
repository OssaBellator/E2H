name: Bootstrap Anthropic Messages ingest

on:
  push:
    branches: [agent/anthropic-messages-ingest]
    paths: [.bootstrap/anthropic-messages-ready]
  pull_request:
    branches: [agent/redaction-policies]
    paths:
      - .bootstrap/anthropic-messages-ready
      - .github/workflows/bootstrap-anthropic-messages.yml

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/anthropic-messages-ingest
          fetch-depth: 0
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.12.2"
          enable-cache: true
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Apply fail-closed integration patch
        run: |
          python bootstrap_anthropic_messages.py
          python fix_anthropic_validation.py
      - name: Sync dependencies
        run: uv sync --extra dev
      - name: Lint and format
        run: |
          uv run ruff check . --fix
          uv run ruff format .
          uv run ruff check .
          uv run ruff format --check .
      - name: Strict typing
        run: uv run mypy src
      - name: Test suite
        run: uv run pytest
      - name: Validate Anthropic provider smoke
        run: |
          mkdir -p .e2h
          uv run e2h ingest anthropic-messages examples/ingest/anthropic-messages.json \
            --output .e2h/anthropic-bundle.json \
            --traces .e2h/anthropic-trace.jsonl \
            --redaction-report .e2h/anthropic-review.json
          uv run python - <<'PY'
          import json
          from pathlib import Path

          bundle = json.loads(Path(".e2h/anthropic-bundle.json").read_text(encoding="utf-8"))
          review = json.loads(Path(".e2h/anthropic-review.json").read_text(encoding="utf-8"))
          rendered = json.dumps(bundle)
          events = bundle["traces"][0]["events"]
          event_types = [event["event_type"] for event in events]
          assert bundle["provenance"]["format"] == "anthropic-messages-json"
          assert bundle["redaction_review"] == review
          assert event_types.count("message.observed") == 5
          assert event_types.count("tool.called") == 1
          assert event_types.count("tool.completed") == 1
          assert len([
              event
              for event in events
              if event["attributes"].get("artifact_kind") == "anthropic.message"
          ]) == 2
          completed = next(event for event in events if event["event_type"] == "tool.completed")
          assert completed["attributes"]["linked_call"] is True
          assert completed["attributes"]["tool_name"] == "lookup_customer"
          thinking = next(
              event
              for event in events
              if event["attributes"].get("artifact_kind") == "anthropic.thinking_presence"
          )
          assert thinking["payload"]["thinking_present"] is True
          assert thinking["payload"]["signature_present"] is True
          assert review["counts_by_kind"] == {"email": 2, "phone": 1, "secret": 1}
          assert review["residual_findings"] == []
          assert "demo@example.com" not in rendered
          assert "+61 412 345 678" not in rendered
          assert "sk-example1234567890" not in rendered
          assert "private reasoning must never enter evidence" not in rendered
          assert "opaque-thinking-signature" not in rendered
          assert len(Path(".e2h/anthropic-trace.jsonl").read_text().splitlines()) == 12
          PY
      - name: Commit tested implementation
        shell: bash
        run: |
          set -euo pipefail
          git rm .bootstrap/anthropic-messages-ready
          rm -rf .e2h
          git add .
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Add Anthropic Messages evidence ingestion"
          git push origin HEAD:agent/anthropic-messages-ingest
